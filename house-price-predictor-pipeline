pipeline{

    agent {label  mlops-worker}

    stages{
        stage('Check environment'){
            steps{

                echo "House price predictor ml modal"
                echo java --version
                echo python --version
                echo docker --version
                echo pwd
            }
        }
        stage('Test WorkSpace'){
            steps{
                echo "Creating a text file in /home/reactserve/jenkins/agent"
                touch connection_test.py
                ls -l  
             }
        }
    }

}