node {
    stage('Build') {
        // Run build commands
        sh 'echo "Building project..."'
    }
    stage('Test') {
        // Run tests
        sh 'echo "Running tests..."'
    }
    stage('Deploy') {
        sh 'echo "Deploying application..."'
    }
    post {
    always {
        emailext (
            to: 'ravichinta1997@gmail.com',
            subject: "Failed: ${env.JOB_NAME} [${env.BUILD_NUMBER}]",
            body: "Check build: ${env.BUILD_URL}"
        )
    }

    }
}
