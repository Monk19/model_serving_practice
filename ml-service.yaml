apiVersion: v1
kind: Service
metadata:
  name: predictor-service
spec:
  selector:
    app: predictor
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
    nodeport: 32000
  type: LoadBalancer
